sudo: false
language: python
python:
    - 2.7

install:
    - pip install pep8

script:
    - num_errors=`find cli/ tests/ -name \*.py -exec pep8 --ignore=E501,E402 {} + | wc -l`
    - |
        if (( $num_errors > 0 )); then
          exit 1;
        fi
    - python setup.py test
